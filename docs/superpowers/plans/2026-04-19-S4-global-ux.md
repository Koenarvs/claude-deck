# S4 — Global UX (Approval Queue + Notifications + Connection Indicator)

**Burst:** Support | **Depends on:** F0 merged | **Branch:** `feat/S4-global-ux`

## Goal
Floating global approval queue (visible on every route), connection indicator, browser notifications, tab-title badge, toast system.

## Spec references
- §10.3 global UI
- §14.4 S4

## Scope
- Create: `src/components/global/GlobalApprovalQueue.tsx` — floats top-right
- Create: `src/components/global/ApprovalCard.tsx`
- Create: `src/components/global/ConnectionIndicator.tsx` — bottom-left
- Create: `src/components/global/ToastContainer.tsx`
- Create: `src/components/global/Toast.tsx`
- Create: `src/lib/notifications.ts` — browser-notifications API wrapper
- Create: `src/lib/tab-badge.ts` — updates `document.title` badge
- Create: `src/hooks/useKeyboardShortcuts.ts`
- Modify: `src/components/AppShell.tsx` (F0) — add GlobalApprovalQueue, ConnectionIndicator, ToastContainer
- Create: `tests/client/global-ux.test.tsx`

## Contracts consumed
- `src/stores/useApprovalsStore`, `useConnectionStore`
- Backend: POST `/api/approvals/:id/decide`

## Recommended task order
1. TDD `notifications.ts`:
   - `requestPermission()` wraps `Notification.requestPermission()`
   - `notify(title, body, options?)` creates a Notification if permission granted; no-op otherwise
   - Handles the `"default"` permission state by returning without error
2. TDD `tab-badge.ts`:
   - `setBadge(count: number)` prefixes `document.title` with `(N) `
   - `clearBadge()` restores original
3. TDD `ToastContainer` + `Toast`: props include `{type: 'info'|'success'|'warn'|'error', message, duration?}`; auto-dismisses after duration; stack up to 5 visible.
4. TDD `ApprovalCard`: props `{approval}`; shows tool name, args (pretty-printed JSON), Allow / Deny buttons; countdown timer showing time until timeout.
5. TDD `GlobalApprovalQueue`: consumes `useApprovalsStore.pending`; renders a stack of ApprovalCards in the top-right. Allow → POST with decision=approved; Deny → POST with decision=denied. Also triggers `notify()` on new approval and `setBadge()` with count.
6. TDD `ConnectionIndicator`: consumes `useConnectionStore.status`; renders a small icon + color: green (open), yellow (connecting), red (error/closed). Position fixed bottom-left.
7. TDD `useKeyboardShortcuts`: registers global listeners — `Cmd/Ctrl+K` opens a command palette (stub: shows a modal that navigates to routes by keyword), `Esc` closes modals, `G then B/D/F/A/S` goto (/board, /dashboard, /feed, /analytics, /settings).
8. Wire all globals into AppShell (F0 gets modified; import the new components, add to the shell layout).

## First-run notification consent
On first `approval:pending` arrival, if notification permission is `default`, show an in-app banner: "Enable browser notifications for approval alerts?" → user clicks Enable → `requestPermission()` called. After permission grant or deny, the banner doesn't reappear.

## Acceptance criteria (spec §13.1 + §14.4 S4)
- [ ] Approval card appears within 500ms of WS `approval:pending` event
- [ ] Click Allow → POST /api/approvals/:id/decide, card disappears within 300ms of 200 response
- [ ] Tab title shows `(N) claude-deck` where N = count of pending approvals
- [ ] Browser notification fires on new pending approval (if permission granted)
- [ ] ConnectionIndicator updates live when WS status changes
- [ ] Toast appears on network errors with clear message
- [ ] Keyboard shortcut `G B` navigates to /board; `Esc` closes any open modal

## QA Checklist
- [ ] **QA-1:** Dispatch WS `approval:pending` event → card appears in top-right within 500ms
- [ ] **QA-2:** Click Allow → POST fires with `{decision: 'approved'}`; card disappears
- [ ] **QA-3:** Tab title prefix reflects pending count; reaches 0 when last approval resolved
- [ ] **QA-4:** Notification permission flow: default state → banner shown; grant → permission=granted; subsequent approvals notify
- [ ] **QA-5:** ConnectionIndicator: simulate WS disconnect → color flips to red within 200ms
- [ ] **QA-6:** Toast appears on a 500 API response with error message; auto-dismisses after 5s
- [ ] **QA-7:** Keyboard: G then B navigates to /board; Esc closes the test modal
- [ ] **QA-8:** Approval countdown timer ticks down (verify at 1Hz)
- [ ] **QA-9:** No `any` types

## Quality bar
- No `any`, accessible focus management (focus trap in modals), screen reader announcements for toasts (aria-live="polite")
