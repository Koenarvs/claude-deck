// ProviderConfig is owned by Phase 1 Delta B (Zod-inferred in src/shared/schemas.ts).
// Re-exported here so analytics code can import it from a stable agents/ path
// without duplicating the shape.
export type { ProviderConfig } from '../schemas';
